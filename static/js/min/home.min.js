/*
$(document).ready(function() {

	// Scroll Animations
	$('.home-section').waypoint(function(direction) {
		if(direction === 'down') {
			//trigger_animation(this);
		}
	},{
		offset: '50%'
	}).waypoint(function(direction) {
		if(direction === 'up') {
			//trigger_animation(this);
		}
	},{
		offset: function(){
			return -$(this).height()/2;
		}
	});

	// Scroll to Animations
	$('.local-navigation a').bind('click',function(e){
		e.preventDefault();
		smooth_scroll($(this).attr('href'));
	});

});
*/function smooth_scroll(e){$("html, body").animate({scrollTop:$(e).offset().top},1e3)}function trigger_animation(e){var t=$(e).attr("id");$("body").removeClass().addClass("color-"+t)};